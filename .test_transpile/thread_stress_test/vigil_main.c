/* Generated by vigil transpile -- do not edit. */
#include "vigil_generated.h"
#include <stdio.h>
#include <stdlib.h>
#include "vigil/transpile_rt.h"
#include "vigil/vigil.h"
#include "vigil/stdlib.h"
#include "vigil/compiler.h"
#include "internal/vigil_internal.h"
#include "plugin_registry.h"

static const char vigil_src_0[] = "// ============================================================================\n// thread_stress_test.vigil — Comprehensive thread module stress test\n//\n// Exercises every available thread primitive:\n//   thread: spawn, join (with return value), detach, current_id, yield, sleep,\n//           mutex, lock, unlock, try_lock, mutex_destroy,\n//           cond, wait, wait_timeout, signal, broadcast, cond_destroy,\n//           rwlock, read_lock, write_lock, rw_unlock, rwlock_destroy\n//   atomic: new, load, store, add, sub, cas, inc, dec\n//\n// Structure:\n//   Section 1 — Sequential baseline (Monte Carlo pi estimation)\n//   Section 2 — Parallel Monte Carlo with 50+ threads + join return values\n//   Section 3 — Producer-consumer pipeline with bounded queue + wait_timeout\n//   Section 4 — Reader-writer lock contention + rwlock_destroy\n//   Section 5 — try_lock spin loop and yield stress + mutex_destroy\n//   Section 6 — CAS-based lock-free counter\n//   Section 7 — Broadcast wakeup (barrier emulation) + cond_destroy\n//   Section 8 — Detached fire-and-forget threads\n//   Section 9 — Result comparison and report\n// ============================================================================\n// ── Configuration ───────────────────────────────────────────────────────────\n\nimport \"atomic\"\nimport \"fmt\"\nimport \"math\"\nimport \"thread\"\nimport \"time\"\n\nconst i64 NUM_WORKERS = i64(50)\n\nconst i64 ITERS_PER_WORKER = i64(200000)\n\nconst i64 NUM_PRODUCERS = i64(8)\n\nconst i64 NUM_CONSUMERS = i64(8)\n\nconst i64 TASKS_PER_PRODUCER = i64(50)\n\nconst i64 QUEUE_CAPACITY = i64(100)\n\nconst i64 RW_READERS = i64(10)\n\nconst i64 RW_WRITERS = i64(4)\n\nconst i64 RW_OPS_PER_THREAD = i64(500)\n\nconst i64 TRYLOCK_THREADS = i64(8)\n\nconst i64 TRYLOCK_ITERS = i64(1000)\n\nconst i64 CAS_THREADS = i64(16)\n\nconst i64 CAS_ITERS = i64(5000)\n\nconst i64 BROADCAST_THREADS = i64(20)\n\nconst i64 DETACH_THREADS = i64(10)\n// ============================================================================\n// Section 1: Sequential Monte Carlo pi estimation (baseline)\n// ============================================================================\nfn lcg_next(i64 state) -> i64 {\n    i64 a = i64(1103515245)\n    i64 c = i64(12345)\n    i64 m = i64(2147483648)\n    return (a * state + c) % m\n}\n\n\nfn monte_carlo_sequential(i64 total_iters) -> i64 {\n    i64 inside = i64(0)\n    i64 rng = i64(42)\n    for (i64 i = i64(0); i < total_iters; i = i + i64(1)) {\n        rng = lcg_next(rng)\n        i64 xi = rng % i64(10000)\n        rng = lcg_next(rng)\n        i64 yi = rng % i64(10000)\n        if xi * xi + yi * yi < i64(100000000) {\n            inside = inside + i64(1)\n        }\n\n    }\n\n    return inside\n}\n\n// ============================================================================\n// Section 2: Parallel Monte Carlo — uses thread.join return values\n//   Workers return their hit count directly; main collects via join.\n// ============================================================================\nfn monte_carlo_worker(i64 iters, i64 seed) -> i64 {\n    i64 inside = i64(0)\n    i64 rng = seed\n    for (i64 i = i64(0); i < iters; i = i + i64(1)) {\n        rng = lcg_next(rng)\n        i64 xi = rng % i64(10000)\n        rng = lcg_next(rng)\n        i64 yi = rng % i64(10000)\n        if xi * xi + yi * yi < i64(100000000) {\n            inside = inside + i64(1)\n        }\n\n    }\n\n    return inside\n}\n\n// ============================================================================\n// Section 3: Producer-consumer pipeline with wait_timeout\n//   Consumers use wait_timeout instead of plain wait for graceful shutdown.\n// ============================================================================\nfn producer_work(i64 id, i64 tasks, i64 q_mtx, i64 q_not_full, i64 q_not_empty, i64 q_head, i64 q_tail, i64 q_count, i64 q_base, i64 produced_total) -> void {\n    for (i64 t = i64(0); t < tasks; t = t + i64(1)) {\n        i64 task_val = id * i64(1000) + t\n        thread.lock(q_mtx)\n        for (; atomic.load(q_count) >= QUEUE_CAPACITY;) {\n            thread.wait(q_not_full, q_mtx)\n        }\n\n        i64 tail = atomic.load(q_tail)\n        i64 slot = q_base + tail\n        atomic.store(slot, task_val)\n        atomic.store(q_tail, (tail + i64(1)) % QUEUE_CAPACITY)\n        atomic.inc(q_count)\n        thread.signal(q_not_empty)\n        thread.unlock(q_mtx)\n    }\n\n    atomic.add(produced_total, tasks)\n}\n\n\nfn consumer_work(i64 q_mtx, i64 q_not_full, i64 q_not_empty, i64 q_head, i64 q_tail, i64 q_count, i64 q_base, i64 done_flag, i64 consumed_sum, i64 consumed_count) -> void {\n    for (;;) {\n        thread.lock(q_mtx)\n        for (; atomic.load(q_count) == i64(0);) {\n            if atomic.load(done_flag) == i64(1) {\n                thread.unlock(q_mtx)\n                return\n            }\n\n            // Use wait_timeout: wake every 10ms to check done_flag\n            thread.wait_timeout(q_not_empty, q_mtx, i64(10))\n        }\n\n        if atomic.load(q_count) == i64(0) {\n            thread.unlock(q_mtx)\n            if atomic.load(done_flag) == i64(1) {\n                return\n            }\n\n            continue\n        }\n\n        i64 head = atomic.load(q_head)\n        i64 slot = q_base + head\n        i64 val = atomic.load(slot)\n        atomic.store(q_head, (head + i64(1)) % QUEUE_CAPACITY)\n        atomic.dec(q_count)\n        thread.signal(q_not_full)\n        thread.unlock(q_mtx)\n        atomic.add(consumed_sum, val)\n        atomic.inc(consumed_count)\n    }\n\n}\n\n// ============================================================================\n// Section 4: Reader-writer lock contention + rwlock_destroy\n// ============================================================================\nfn reader_work(i64 rw, i64 shared_val, i64 read_sum, i64 ops) -> void {\n    for (i64 i = i64(0); i < ops; i = i + i64(1)) {\n        thread.read_lock(rw)\n        i64 v = atomic.load(shared_val)\n        thread.rw_unlock(rw)\n        atomic.add(read_sum, v)\n    }\n\n}\n\n\nfn writer_work(i64 rw, i64 shared_val, i64 ops) -> void {\n    for (i64 i = i64(0); i < ops; i = i + i64(1)) {\n        thread.write_lock(rw)\n        atomic.inc(shared_val)\n        thread.rw_unlock(rw)\n    }\n\n}\n\n// ============================================================================\n// Section 5: try_lock spin loop + yield stress + mutex_destroy\n// ============================================================================\nfn trylock_worker(i64 mtx, i64 counter, i64 iters) -> void {\n    for (i64 i = i64(0); i < iters; i = i + i64(1)) {\n        for (;! thread.try_lock(mtx);) {\n            thread.yield()\n        }\n\n        atomic.inc(counter)\n        thread.unlock(mtx)\n    }\n\n}\n\n// ============================================================================\n// Section 6: CAS-based lock-free counter\n// ============================================================================\nfn cas_worker(i64 counter, i64 iters) -> void {\n    for (i64 i = i64(0); i < iters; i = i + i64(1)) {\n        for (;;) {\n            i64 old = atomic.load(counter)\n            if atomic.cas(counter, old, old + i64(1)) {\n                break\n            }\n\n        }\n\n    }\n\n}\n\n// ============================================================================\n// Section 7: Broadcast barrier emulation + cond_destroy\n// ============================================================================\nfn barrier_worker(i64 bar_mtx, i64 bar_cond, i64 arrived, i64 total, i64 result_counter) -> void {\n    thread.lock(bar_mtx)\n    atomic.inc(arrived)\n    if atomic.load(arrived) == total {\n        thread.broadcast(bar_cond)\n    } else {\n        for (; atomic.load(arrived) < total;) {\n            thread.wait(bar_cond, bar_mtx)\n        }\n\n    }\n\n    thread.unlock(bar_mtx)\n    atomic.inc(result_counter)\n}\n\n// ============================================================================\n// Section 8: Detached fire-and-forget threads\n//   Uses thread.detach — threads write to an atomic before exiting.\n// ============================================================================\nfn detach_worker(i64 counter) -> void {\n    thread.sleep(i64(5))\n    atomic.inc(counter)\n}\n\n// ============================================================================\n// Main — run all sections, compare, report\n// ============================================================================\nfn main() -> i32 {\n    fmt.println(\"=== VIGIL Thread Stress Test ===\")\n    fmt.println(\"\")\n    i64 total_iters = NUM_WORKERS * ITERS_PER_WORKER\n    // ── Section 1: Sequential baseline ──────────────────────────────────\n    fmt.println(\"[1] Sequential Monte Carlo pi estimation...\")\n    i64 t0 = time.now_ms()\n    i64 seq_hits = monte_carlo_sequential(total_iters)\n    i64 t1 = time.now_ms()\n    i64 seq_ms = t1 - t0\n    fmt.println(f\"    Iterations: {total_iters}\")\n    fmt.println(f\"    Hits: {seq_hits}\")\n    fmt.println(f\"    Time: {seq_ms} ms\")\n    // ── Section 2: Parallel Monte Carlo with join return values ─────────\n    fmt.println(\"\")\n    fmt.println(f\"[2] Parallel Monte Carlo ({NUM_WORKERS} threads, join return values)...\")\n    i64 t2 = time.now_ms()\n    // Allocate handle storage\n    array<i64> handles = []\n    for (i64 i = i64(0); i < NUM_WORKERS; i = i + i64(1)) {\n        i64 seed = i64(1000) + i * i64(7919)\n        i64 iters = ITERS_PER_WORKER\n        i64 t = thread.spawn(\n            fn () -> i64 {\n                return monte_carlo_worker(iters, seed)\n            }\n\n        )\n        handles.push(t)\n    }\n\n    // Collect results via join return values\n    i64 par_result = i64(0)\n    for (i32 i = 0; i < handles.len(); i++) {\n        i64 hits = thread.join(handles[i])\n        par_result = par_result + hits\n    }\n\n    i64 t3 = time.now_ms()\n    i64 par_ms = t3 - t2\n    fmt.println(f\"    Hits: {par_result}\")\n    fmt.println(f\"    Time: {par_ms} ms\")\n    if par_ms > i64(0) {\n        fmt.println(f\"    Speedup: ~{seq_ms / par_ms}x\")\n    }\n\n    // ── Section 3: Producer-consumer pipeline (wait_timeout) ────────────\n    fmt.println(\"\")\n    fmt.println(\"[3] Producer-consumer pipeline (wait_timeout)...\")\n    fmt.println(f\"    {NUM_PRODUCERS} producers x {TASKS_PER_PRODUCER} tasks, {NUM_CONSUMERS} consumers, queue cap {QUEUE_CAPACITY}\")\n    i64 q_mtx = thread.mutex()\n    i64 q_not_full = thread.cond()\n    i64 q_not_empty = thread.cond()\n    i64 q_head = atomic.new(i64(0))\n    i64 q_tail = atomic.new(i64(0))\n    i64 q_count = atomic.new(i64(0))\n    i64 q_base = atomic.new(i64(0))\n    for (i64 i = i64(1); i < QUEUE_CAPACITY; i = i + i64(1)) {\n        atomic.new(i64(0))\n    }\n\n    i64 done_flag = atomic.new(i64(0))\n    i64 produced_total = atomic.new(i64(0))\n    i64 consumed_sum = atomic.new(i64(0))\n    i64 consumed_count = atomic.new(i64(0))\n    i64 t4 = time.now_ms()\n    // Spawn producers\n    array<i64> prod_handles = []\n    for (i64 i = i64(0); i < NUM_PRODUCERS; i = i + i64(1)) {\n        i64 pid = i\n        i64 tasks = TASKS_PER_PRODUCER\n        i64 pt = produced_total\n        i64 t = thread.spawn(\n            fn () -> void {\n                producer_work(pid, tasks, q_mtx, q_not_full, q_not_empty, q_head, q_tail, q_count, q_base, pt)\n            }\n\n        )\n        prod_handles.push(t)\n    }\n\n    // Spawn consumers\n    array<i64> cons_handles = []\n    for (i64 i = i64(0); i < NUM_CONSUMERS; i = i + i64(1)) {\n        i64 t = thread.spawn(\n            fn () -> void {\n                consumer_work(q_mtx, q_not_full, q_not_empty, q_head, q_tail, q_count, q_base, done_flag, consumed_sum, consumed_count)\n            }\n\n        )\n        cons_handles.push(t)\n    }\n\n    for (i32 i = 0; i < prod_handles.len(); i++) {\n        thread.join(prod_handles[i])\n    }\n\n    // Signal done — consumers will notice via wait_timeout, no broadcast needed\n    atomic.store(done_flag, i64(1))\n    for (i32 i = 0; i < cons_handles.len(); i++) {\n        thread.join(cons_handles[i])\n    }\n\n    i64 t5 = time.now_ms()\n    i64 pc_ms = t5 - t4\n    i64 total_produced = atomic.load(produced_total)\n    i64 total_consumed = atomic.load(consumed_count)\n    i64 total_sum = atomic.load(consumed_sum)\n    i64 expected_sum = i64(0)\n    for (i64 p = i64(0); p < NUM_PRODUCERS; p = p + i64(1)) {\n        for (i64 t = i64(0); t < TASKS_PER_PRODUCER; t = t + i64(1)) {\n            expected_sum = expected_sum + p * i64(1000) + t\n        }\n\n    }\n\n    fmt.println(f\"    Produced: {total_produced}, Consumed: {total_consumed}\")\n    fmt.println(f\"    Sum: {total_sum} (expected: {expected_sum})\")\n    fmt.println(f\"    Time: {pc_ms} ms\")\n    if total_consumed != total_produced {\n        fmt.println(\"    *** FAIL: produced != consumed ***\")\n    }\n\n    if total_sum != expected_sum {\n        fmt.println(\"    *** FAIL: sum mismatch ***\")\n    }\n\n    // Clean up queue synchronization primitives\n    thread.mutex_destroy(q_mtx)\n    thread.cond_destroy(q_not_full)\n    thread.cond_destroy(q_not_empty)\n    // ── Section 4: Reader-writer lock contention + destroy ──────────────\n    fmt.println(\"\")\n    fmt.println(\"[4] Reader-writer lock contention...\")\n    i64 rw = thread.rwlock()\n    i64 shared_val = atomic.new(i64(0))\n    i64 read_sum = atomic.new(i64(0))\n    i64 t6 = time.now_ms()\n    array<i64> rw_handles = []\n    for (i64 i = i64(0); i < RW_READERS; i = i + i64(1)) {\n        i64 ops = RW_OPS_PER_THREAD\n        i64 t = thread.spawn(\n            fn () -> void {\n                reader_work(rw, shared_val, read_sum, ops)\n            }\n\n        )\n        rw_handles.push(t)\n    }\n\n    for (i64 i = i64(0); i < RW_WRITERS; i = i + i64(1)) {\n        i64 ops = RW_OPS_PER_THREAD\n        i64 t = thread.spawn(\n            fn () -> void {\n                writer_work(rw, shared_val, ops)\n            }\n\n        )\n        rw_handles.push(t)\n    }\n\n    for (i32 i = 0; i < rw_handles.len(); i++) {\n        thread.join(rw_handles[i])\n    }\n\n    i64 t7 = time.now_ms()\n    i64 rw_ms = t7 - t6\n    i64 final_shared = atomic.load(shared_val)\n    i64 expected_writes = RW_WRITERS * RW_OPS_PER_THREAD\n    fmt.println(f\"    Writers: {RW_WRITERS} x {RW_OPS_PER_THREAD} ops = {expected_writes} expected increments\")\n    fmt.println(f\"    Final shared value: {final_shared}\")\n    fmt.println(f\"    Time: {rw_ms} ms\")\n    if final_shared != expected_writes {\n        fmt.println(\"    *** FAIL: write count mismatch ***\")\n    }\n\n    thread.rwlock_destroy(rw)\n    // ── Section 5: try_lock spin + yield + mutex_destroy ────────────────\n    fmt.println(\"\")\n    fmt.println(\"[5] try_lock spin + yield stress...\")\n    i64 tl_mtx = thread.mutex()\n    i64 tl_counter = atomic.new(i64(0))\n    i64 t8 = time.now_ms()\n    array<i64> tl_handles = []\n    for (i64 i = i64(0); i < TRYLOCK_THREADS; i = i + i64(1)) {\n        i64 iters = TRYLOCK_ITERS\n        i64 t = thread.spawn(\n            fn () -> void {\n                trylock_worker(tl_mtx, tl_counter, iters)\n            }\n\n        )\n        tl_handles.push(t)\n    }\n\n    for (i32 i = 0; i < tl_handles.len(); i++) {\n        thread.join(tl_handles[i])\n    }\n\n    i64 t9 = time.now_ms()\n    i64 tl_ms = t9 - t8\n    i64 tl_result = atomic.load(tl_counter)\n    i64 tl_expected = TRYLOCK_THREADS * TRYLOCK_ITERS\n    fmt.println(f\"    Counter: {tl_result} (expected: {tl_expected})\")\n    fmt.println(f\"    Time: {tl_ms} ms\")\n    if tl_result != tl_expected {\n        fmt.println(\"    *** FAIL: try_lock counter mismatch ***\")\n    }\n\n    thread.mutex_destroy(tl_mtx)\n    // ── Section 6: CAS-based lock-free counter ──────────────────────────\n    fmt.println(\"\")\n    fmt.println(\"[6] CAS lock-free counter...\")\n    i64 cas_counter = atomic.new(i64(0))\n    i64 t10 = time.now_ms()\n    array<i64> cas_handles = []\n    for (i64 i = i64(0); i < CAS_THREADS; i = i + i64(1)) {\n        i64 iters = CAS_ITERS\n        i64 t = thread.spawn(\n            fn () -> void {\n                cas_worker(cas_counter, iters)\n            }\n\n        )\n        cas_handles.push(t)\n    }\n\n    for (i32 i = 0; i < cas_handles.len(); i++) {\n        thread.join(cas_handles[i])\n    }\n\n    i64 t11 = time.now_ms()\n    i64 cas_ms = t11 - t10\n    i64 cas_result = atomic.load(cas_counter)\n    i64 cas_expected = CAS_THREADS * CAS_ITERS\n    fmt.println(f\"    Counter: {cas_result} (expected: {cas_expected})\")\n    fmt.println(f\"    Time: {cas_ms} ms\")\n    if cas_result != cas_expected {\n        fmt.println(\"    *** FAIL: CAS counter mismatch ***\")\n    }\n\n    // ── Section 7: Broadcast barrier + cond_destroy ─────────────────────\n    fmt.println(\"\")\n    fmt.println(f\"[7] Broadcast barrier ({BROADCAST_THREADS} threads)...\")\n    i64 bar_mtx = thread.mutex()\n    i64 bar_cond = thread.cond()\n    i64 bar_arrived = atomic.new(i64(0))\n    i64 bar_result = atomic.new(i64(0))\n    i64 t12 = time.now_ms()\n    array<i64> bar_handles = []\n    for (i64 i = i64(0); i < BROADCAST_THREADS; i = i + i64(1)) {\n        i64 t = thread.spawn(\n            fn () -> void {\n                barrier_worker(bar_mtx, bar_cond, bar_arrived, BROADCAST_THREADS, bar_result)\n            }\n\n        )\n        bar_handles.push(t)\n    }\n\n    for (i32 i = 0; i < bar_handles.len(); i++) {\n        thread.join(bar_handles[i])\n    }\n\n    i64 t13 = time.now_ms()\n    i64 bar_ms = t13 - t12\n    i64 bar_count = atomic.load(bar_result)\n    fmt.println(f\"    Threads past barrier: {bar_count} (expected: {BROADCAST_THREADS})\")\n    fmt.println(f\"    Time: {bar_ms} ms\")\n    if bar_count != BROADCAST_THREADS {\n        fmt.println(\"    *** FAIL: not all threads passed barrier ***\")\n    }\n\n    thread.mutex_destroy(bar_mtx)\n    thread.cond_destroy(bar_cond)\n    // ── Section 8: Detached fire-and-forget threads ─────────────────────\n    fmt.println(\"\")\n    fmt.println(f\"[8] Detached threads ({DETACH_THREADS} fire-and-forget)...\")\n    i64 detach_counter = atomic.new(i64(0))\n    i64 t14 = time.now_ms()\n    for (i64 i = i64(0); i < DETACH_THREADS; i = i + i64(1)) {\n        i64 t = thread.spawn(\n            fn () -> void {\n                detach_worker(detach_counter)\n            }\n\n        )\n        thread.detach(t)\n    }\n\n    // Give detached threads time to finish (they sleep 5ms + do work)\n    thread.sleep(i64(100))\n    i64 t15 = time.now_ms()\n    i64 detach_ms = t15 - t14\n    i64 detach_count = atomic.load(detach_counter)\n    fmt.println(f\"    Completed: {detach_count} (expected: {DETACH_THREADS})\")\n    fmt.println(f\"    Time: {detach_ms} ms\")\n    if detach_count != DETACH_THREADS {\n        fmt.println(\"    *** FAIL: not all detached threads completed ***\")\n    }\n\n    // ── Section 9: Summary ──────────────────────────────────────────────\n    fmt.println(\"\")\n    fmt.println(\"=== Summary ===\")\n    i64 failures = i64(0)\n    if seq_hits == i64(0) {\n        fmt.println(\"FAIL: Sequential hits = 0\")\n        failures = failures + i64(1)\n    }\n\n    if par_result == i64(0) {\n        fmt.println(\"FAIL: Parallel hits = 0\")\n        failures = failures + i64(1)\n    }\n\n    if total_consumed != total_produced {\n        fmt.println(\"FAIL: Producer-consumer count mismatch\")\n        failures = failures + i64(1)\n    }\n\n    if total_sum != expected_sum {\n        fmt.println(\"FAIL: Producer-consumer sum mismatch\")\n        failures = failures + i64(1)\n    }\n\n    if final_shared != expected_writes {\n        fmt.println(\"FAIL: RWLock write count mismatch\")\n        failures = failures + i64(1)\n    }\n\n    if tl_result != tl_expected {\n        fmt.println(\"FAIL: try_lock counter mismatch\")\n        failures = failures + i64(1)\n    }\n\n    if cas_result != cas_expected {\n        fmt.println(\"FAIL: CAS counter mismatch\")\n        failures = failures + i64(1)\n    }\n\n    if bar_count != BROADCAST_THREADS {\n        fmt.println(\"FAIL: Barrier count mismatch\")\n        failures = failures + i64(1)\n    }\n\n    if detach_count != DETACH_THREADS {\n        fmt.println(\"FAIL: Detached thread count mismatch\")\n        failures = failures + i64(1)\n    }\n\n    if failures == i64(0) {\n        fmt.println(\"ALL CHECKS PASSED\")\n    } else {\n        fmt.println(f\"{failures} CHECK(S) FAILED\")\n    }\n\n    i64 total_threads = NUM_WORKERS + NUM_PRODUCERS + NUM_CONSUMERS + RW_READERS + RW_WRITERS + TRYLOCK_THREADS + CAS_THREADS + BROADCAST_THREADS + DETACH_THREADS\n    fmt.println(f\"Total threads spawned: {total_threads}\")\n    i64 total_ms = time.now_ms() - t0\n    fmt.println(f\"Total wall time: {total_ms} ms\")\n    if failures > i64(0) {\n        return 1\n    }\n\n    return 0\n}\n";
static const char vigil_path_0[] = "examples/thread_stress_test.vigil";

int main(void)
{
    vigil_runtime_t *runtime = NULL;
    vigil_vm_t *vm = NULL;
    vigil_error_t error = {0};
    vigil_source_registry_t registry;
    vigil_diagnostic_list_t diagnostics;
    vigil_object_t *function = NULL;
    vigil_source_id_t sid = 0;
    vigil_tc_t tc = {0};
    if (vigil_runtime_open(&runtime, NULL, &error) != 0) return 1;
    if (vigil_vm_open(&vm, runtime, NULL, &error) != 0)
    { vigil_runtime_close(&runtime); return 1; }
    vigil_vm_set_aot_enabled(vm, 0);
    vigil_source_registry_init(&registry, runtime);
    vigil_diagnostic_list_init(&diagnostics, runtime);
    { vigil_source_id_t _id = 0;
      vigil_source_registry_register_cstr(&registry, vigil_path_0, vigil_src_0, &_id, &error);
      if (0 == 0) sid = _id; }
    { vigil_native_registry_t natives;
      vigil_native_registry_init(&natives);
      vigil_stdlib_register_all(&natives, &error);
      vigil_plugin_register_all(&natives, &error);
      vigil_compile_source_with_natives(&registry, sid, &natives, &function, &diagnostics, &error);
      vigil_native_registry_free(&natives); }
    if (!function) { vigil_diagnostic_list_free(&diagnostics);
      vigil_source_registry_free(&registry); vigil_vm_close(&vm);
      vigil_runtime_close(&runtime); return 1; }
    tc.vm = vm; tc.runtime = runtime;
    tc.function = function;
    { size_t _fi;
      for (_fi = 0; _fi < 21; _fi++) {
        const vigil_object_t *_fn = vigil_function_object_sibling(function, _fi);
        if (!_fn) _fn = function;
        const vigil_chunk_t *_ch = vigil_function_object_chunk(_fn);
        vigil_fn_constants[_fi] = _ch ? (vigil_value_t*)vigil_chunk_constant(_ch, 0) : NULL;
        vigil_fn_constant_counts[_fi] = _ch ? vigil_chunk_constant_count(_ch) : 0;
      }
    }
    tc.constants = vigil_fn_constants[11];
    tc.constant_count = vigil_fn_constant_counts[11];
    vigil_reg_t result = vigil_fn_11(&tc);
    vigil_vm_close(&vm);
    vigil_object_release(&function);
    vigil_diagnostic_list_free(&diagnostics);
    vigil_source_registry_free(&registry);
    vigil_runtime_close(&runtime);
    return (int)result.i;
}
