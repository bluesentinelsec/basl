import "fmt"
import "readline"

fn main() -> i32 {
    i32 n = 1
    string line = readline.input("")
    while line.any() {
        fmt.println(f"     {n}\t{line}")
        n++
        line = readline.input("")
    }

    return 0
}
